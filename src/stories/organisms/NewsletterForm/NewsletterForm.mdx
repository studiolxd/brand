import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as NewsletterFormStories from "./NewsletterForm.stories";

<Meta of={NewsletterFormStories} />

# NewsletterForm

Organismo que compone la molécula `Form` con un campo de email, un checkbox de privacidad y un botón de envío. El estado de éxito vive aquí, no en `Form`. El heading es responsabilidad del contexto que lo incluya.

Todos los estilos heredan de los tokens `--form-*` compartidos. No tiene tokens propios.

## Por defecto

<Canvas of={NewsletterFormStories.Default} />

## Enviando

El botón muestra el texto alternativo y queda deshabilitado mientras se procesa el envío.

<Canvas of={NewsletterFormStories.Submitting} />

## With Errors

<Canvas of={NewsletterFormStories.WithErrors} />

## Success

Cuando `success` es `true`, el formulario se reemplaza por el mensaje de confirmación.

<Canvas of={NewsletterFormStories.Success} />

## Dark

<Canvas of={NewsletterFormStories.Dark} className="bg-dark" />

## Dark With Errors

<Canvas of={NewsletterFormStories.DarkWithErrors} className="bg-dark" />

---

## Props

<ArgTypes of={NewsletterFormStories} />
