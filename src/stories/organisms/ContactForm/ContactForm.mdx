import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as ContactFormStories from "./ContactForm.stories";

<Meta of={ContactFormStories} />

# ContactForm

Organismo que compone la molécula `Form` con un campo de email, un textarea de mensaje y un checkbox de privacidad. El estado de éxito vive aquí, no en `Form`. El heading es responsabilidad del contexto que lo incluya.

Todos los estilos heredan de los tokens `--form-*` compartidos. No tiene tokens propios.

## Por defecto

<Canvas of={ContactFormStories.Default} />

## Enviando

El botón muestra el texto alternativo y queda deshabilitado mientras se procesa el envío.

<Canvas of={ContactFormStories.Submitting} />

## With Errors

<Canvas of={ContactFormStories.WithErrors} />

## Success

Cuando `success` es `true`, el formulario se reemplaza por el mensaje de confirmación.

<Canvas of={ContactFormStories.Success} />

## Dark

<Canvas of={ContactFormStories.Dark} className="bg-dark" />

## Dark With Errors

<Canvas of={ContactFormStories.DarkWithErrors} className="bg-dark" />

---

## Props

<ArgTypes of={ContactFormStories} />
