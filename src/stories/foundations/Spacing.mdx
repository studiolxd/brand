import { Meta } from "@storybook/addon-docs/blocks";
import { SpacingScale } from "./SpacingScale";
import { TokenTable } from "./TokenTable";
import { spacingTokens } from "../../tokens/spacing";

<Meta title="Foundations/Espaciado" />

# Espaciado

Escala de espaciado basada en múltiplos de 4px. Los tokens están numerados para permitir la inserción de valores intermedios sin romper la nomenclatura.

<SpacingScale tokens={spacingTokens} />

---

## Tokens CSS

<TokenTable tokens={spacingTokens} />

Los tokens están definidos en `tokens/spacing/scale.json`.
