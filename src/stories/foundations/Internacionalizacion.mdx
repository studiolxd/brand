import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Internacionalización" />

# Internacionalización

Los componentes de la librería emiten textos por su cuenta: etiquetas accesibles (`aria-label`,
texto para lectores de pantalla) y, en algunos casos, texto visible en pantalla — un mensaje de
"sin resultados", el sumario de una paginación, la zona de arrastre de un campo de archivos.

Hay **dos formas** de que esos textos lleguen traducidos, y el sistema está migrando de la
primera a la segunda:

1. **Props de texto con default castellano** — la convención histórica, y todavía la de la
   inmensa mayoría de componentes.
2. **El proveedor de textos** (`BrandMessagesProvider`) — el destino: el texto sale del catálogo
   de la aplicación y **no hay default**, así que olvidarlo es un error, no un castellano mudo.
   Hoy lo usan `Pagination` y la **familia de tablas** (`Table`, `DataTable`).

## El proveedor de textos

Se monta **una vez en la raíz de la aplicación**, con el catálogo del idioma vigente:

```tsx
import { BrandMessagesProvider } from '@studiolxd/brand/messages';

<BrandMessagesProvider
  messages={{
    pagination: {
      label: t('pagination.label'),
      pagesGroup: t('pagination.pagesGroup'),
      previous: t('pagination.previous'),
      next: t('pagination.next'),
      goToPage: (page) => t('pagination.goToPage', { page }),
      perPage: t('pagination.perPage'),
      total: (count) => t('pagination.total', { count }),
      allOption: t('pagination.allOption'),
    },
    table: {
      actions: t('table.actions'),
      sortable: t('table.sortable'),
      sortedAscending: t('table.sortedAscending'),
      sortedDescending: t('table.sortedDescending'),
    },
    dataTable: {
      empty: t('dataTable.empty'),
      search: t('common.search.placeholder'),
    },
  }}
>
  {children}
</BrandMessagesProvider>
```

El catálogo va creciendo ola a ola: además de `pagination`, `table` y `dataTable`, hoy están los
espacios de los átomos de formulario —`inputField`, `passwordField`, `select`, `multiSelect`,
`numberInput`, `otpInput`, `inputPhone`, `asyncSelect`, `asyncMultiSelect`, `searchForm`,
`docsSearch` y `filterBar`— y los de fecha y hora —`calendar`, `datePicker` y `timeSelect`—. No hace falta memorizarlos: el tipo `BrandMessages` los lleva todos y
**una app que se deje uno no compila**. La tabla de más abajo dice qué prop cae en qué clave.

A partir de ahí, un componente migrado se monta **sin una sola prop de texto**:

```tsx
<Pagination total={200} page={3} pageSize={10} showTotal onPageChange={setPage} />
```

### Cómo está hecho

- **El tipo nace en el DS.** `BrandMessages` es el contrato, y el catálogo de la app es quien lo
  satisface — nunca al revés. Si el tipo se generase desde el JSON de una suite, el DS dependería
  de un paquete de producto para declarar su propio contrato.
- **Anidado por componente, no plano.** Un espacio por componente (`pagination`, `table`,
  `dataTable`, y los que vengan), calcando los que el catálogo de la suite ya tiene. Son 310 textos: una lista plana de
  claves sueltas no se puede escribir ni revisar.
- **Todas las claves son obligatorias** dentro de su espacio, como en `RecoveryCodesLabels`. Una
  app que se deje una **no compila**: ese es el punto entero del mecanismo.
- **La interfaz de cada espacio vive junto a su componente** (`PaginationMessages` se exporta
  desde `Pagination`), y `BrandMessages` solo las ensambla.

### El orden: prop → proveedor → error

| Escalón | Cuándo |
| --- | --- |
| **La prop** | Cuando se pasa. Es la anulación puntual, para el uso que de verdad no dice lo mismo que todos los demás |
| **El proveedor** | Cuando la prop falta |
| **Un error** | Cuando faltan las dos, con el nombre del texto que falta |

**No hay cuarto escalón.** Un default castellano viaja dentro del paquete y se alcanza en
ejecución: pinta "Página 3" dentro de una interfaz en francés sin que falle nada, sin que lo vea
ningún test, y se ve bien estando mal. El error se lanza **donde el texto se pinta**, así que un
componente no exige el texto de una pieza que no enseña.

### Y el Storybook, ¿no es un default por la puerta de atrás?

No, y la diferencia es de sitio, no de matiz. El Storybook es **la aplicación que monta los
componentes**, así que monta el proveedor como lo hará cada app: un decorador global
(`.storybook/preview.tsx`) lo envuelve todo con un fixture castellano
(`.storybook/brandMessagesFixture.ts`).

Ese fixture **no puede llegar a un consumidor**: vive fuera de `src/`, no es punto de entrada de
la librería, ningún componente lo importa y `package.json#files` solo publica `dist/`,
`src/tokens/` y el `CHANGELOG`. Las cuatro cosas las vigila un test
(`src/messages/BrandMessages.test.ts`), no un comentario que lo prometa. Un default, en cambio,
viaja dentro del `.js` compilado y se alcanza desde cualquier app que olvide un texto.

## La convención histórica (props con default)

Mientras el componente no esté migrado al proveedor, cada texto que emite por su cuenta es una
**prop opcional con el texto castellano como valor por defecto**. Una aplicación multiidioma que
no las pase **pierde sus traducciones**: un usuario de lector de pantalla en inglés o en alemán
oye "Página 3" o ve "Sin resultados" en medio de una interfaz traducida — el fallo silencioso que
el proveedor viene a cerrar.

Cada texto que el componente emite por su cuenta es una **prop opcional con el texto castellano
como valor por defecto**:

```tsx
// sin props → castellano
<Modal open={open} onOpenChange={setOpen} title="Ajustes" />

// con props → los textos del consumidor
<Modal
  open={open}
  onOpenChange={setOpen}
  title={t('settings.title')}
  closeLabel={t('common.close')}
  fallbackTitle={t('common.dialog')}
/>
```

Reglas de la convención:

- **El default es siempre el texto castellano actual**, así que añadir la prop nunca es un
  breaking change ni cambia el marcado de quien no la pasa.
- **Nombre `<cosa>Label`** para etiquetas accesibles y textos cortos; `<cosa>Message` para
  mensajes; `<cosa>Error` para textos de error; `<cosa>Hint`/`<cosa>Placeholder` para pistas.
- **Función en vez de string** cuando el texto interpola un valor:
  `pageLabel?: (page: number) => string`, no un string con marcador.
- **Listas de opciones se traducen pasando la lista entera**, no con una prop de texto por
  elemento: `pageSizeOptions` en `Pagination`, `legendItems` en `CalendarRoster`.
- **El JSDoc de la prop dice cuál es el default y que es castellano**, para que se vea desde el
  autocompletado del editor.

## Fechas y números

Los componentes de calendario no traducen nombres de mes y día con props de texto: reciben una
prop **`locale`** (default `'es-ES'`) y delegan en `Intl.DateTimeFormat`. Las props de texto de
esos componentes cubren solo lo que `Intl` no da — los `aria-label` de navegación, la leyenda.

```tsx
<Calendar locale="de-DE" previousMonthLabel="Vorheriger Monat" nextMonthLabel="Nächster Monat" />
```

```tsx
<Calendar locale="de-DE" />   {/* las flechas salen de `calendar.*` del proveedor */}
```

### Una máscara tiene dos mitades, y no se deciden en el mismo sitio

`dd/mm/aaaa` parece una sola cosa. No lo es, y la campaña del proveedor obliga a separarlas:

- **El orden y el separador son formato.** `25/09/2026` en `es`, `09/25/2026` en `en-US` y
  `25.09.2026` en `de` salen de `Intl.DateTimeFormat().formatToParts()`. No son prop, no están
  en el catálogo y no se cablean por idioma: dependen del `locale`, que es de qué convención de
  escritura se habla, no de en qué idioma está la interfaz. Una app inglesa que enseña fechas
  españolas las quiere en orden español.
- **Las letras son idioma.** `aaaa` es «año», `yyyy` es *year*, `jjjj` es *Jahr*; `HH`/`MM` son
  hora y minuto, y en alemán `SS`/`MM`. Son la abreviatura de una palabra, así que se traducen:
  **son cromo y van al catálogo**, como cualquier otro rótulo.

Por eso el catálogo guarda las **letras sueltas** y nunca la máscara montada — la máscara la
arma el componente poniendo las letras en el orden del locale:

```tsx
// mismo catálogo, dos locales: las letras no se mueven, el orden sí
<DatePicker locale="es-ES" />   {/* dd/mm/yyyy */}
<DatePicker locale="en-US" />   {/* mm/dd/yyyy */}
```

La regla, en una frase: **si cambia al cambiar de idioma, es cromo y va al catálogo; si cambia
al cambiar de país, es formato y va por `locale`.**

### «2,5 MB» tiene dos mitades, y una lista necesita la conjunción del idioma

La misma regla, aplicada a la familia de subidas:

- **El peso.** «máx. 2,5 MB» son dos cosas. La frase es idioma y va al catálogo, **como
  plantilla**; la cifra es formato y la escribe `Intl.NumberFormat` con `style: "unit"` — el
  separador decimal, la agrupación y el espacio antes del símbolo salen del `locale`. El
  símbolo `MB` no se traduce: es una unidad, no una palabra. **Al catálogo no baja nunca un
  «2.5 MB» escrito a mano**, que metería el punto inglés en una interfaz española.
- **La lista de formatos.** «JPEG, PNG o WEBP» no es un `join(", ")` con una «o» pegada: la
  conjunción, la coma de Oxford y el espaciado cambian con la lengua, y la lista acaba dentro
  de una frase traducida. Es `Intl.ListFormat` con `type: "disjunction"`.

```tsx
// mismo catálogo inglés, dos locales
<FileUpload locale="es-ES" maxSize={2621440} />   {/* max. 2,5 MB */}
<FileUpload locale="en-US" maxSize={2621440} />   {/* max. 2.5 MB */}
```

## Componentes con props de texto

| Componente | Props |
| --- | --- |
| `Pagination` | **Migrado al proveedor** (`pagination.*`, sin defaults). Las props `ariaLabel`, `pageLabel`, `previousLabel`, `nextLabel`, `pagesGroupLabel`, `pageSizeLabel` y `totalLabel` siguen existiendo como anulación puntual; `pageSizeOptions` sustituye la lista entera del selector |
| `AsyncSelect`, `AsyncSelectField` | **Migrado al proveedor** (`asyncSelect.*`, sin defaults). `placeholder`, `emptyMessage`, `loadingLabel` y `clearLabel` siguen como anulación puntual; `AsyncSelectField` las reenvía |
| `AsyncMultiSelect`, `AsyncMultiSelectField` | **Migrado al proveedor** (`asyncMultiSelect.*`, sin defaults). `placeholder`, `emptyMessage`, `loadingLabel` y `removeLabel` siguen como anulación puntual; `AsyncMultiSelectField` las reenvía |
| `FileUpload`, `FileUploadField` | **Migrado al proveedor** (`fileUpload.dropzone`, `.dropzoneActive`, `.dropzoneHint`, `.maxSize`, `.maxFiles`, `.files`, `.progress`, `.removeFile`, `.tooLarge`, `.invalidType`, sin defaults). Las diez props siguen como anulación puntual. Las dos pistas de límite y el error de peso son **plantillas** que reciben el peso **ya escrito por `Intl.NumberFormat`** con el `locale`: la frase es idioma, la cifra es formato (ver § «"2,5 MB" tiene dos mitades»). `FileUploadField` no emite texto propio: reenvía todo por contexto, y `label`, `helperText` y `errorMessage` son su contenido |
| `AvatarUpload` | **Migrado al proveedor** (`avatarUpload.button`, `.buttonFor`, `.subject`, `.dropHint`, `.dropActive`, `.maxSize`, `.invalidType`, `.tooLarge`, `.cropCancel`, `.cropConfirm`, sin defaults). **El par visible/accesible del botón va entero en el catálogo**: WCAG 2.5.3 exige que el nombre accesible contenga el visible, y partirlo entre el catálogo común y el de la pantalla dejaría los dos lados del contrato en ficheros distintos. `buttonFor` es una **plantilla** —el orden del verbo y del sujeto es del idioma— y el catálogo lleva la regla escrita: `buttonFor(x)` contiene `button`. Fuera del catálogo quedan `subject` (qué se sube: es de la pantalla) y `cropTitle` (obligatorio y sin default, como el `title` del `ImageCropDialog`). `cropCloseLabel`, `cropLoadingLabel` y `cropErrorMessage` son reenvíos puros al diálogo |
| `ImageCropDialog` | **Migrado al proveedor** (`imageCropDialog.loading`, `.error`, sin defaults) — `title`, `cancelLabel` y `confirmLabel` siguen siendo obligatorias y sin default, y **no** van al catálogo: el diálogo no sabe qué se está recortando ni qué pasa al confirmar, y eso es contenido de la pantalla. Es el ejemplo canónico de la línea cromo/contenido. `closeLabel` es reenvío puro al `Modal`, que aún no lee del proveedor |
| `NumberInput`, `NumberInputField` | **Migrado al proveedor** (`numberInput.decrement`, `numberInput.increment`, sin defaults). Las props siguen como anulación puntual |
| `OtpInput`, `OtpField` | **Migrado al proveedor** (`otpInput.digit`, `otpInput.group`, sin defaults). `digitLabel` nombra cada celda y `groupLabel` el grupo del control suelto (dentro de `OtpField` lo nombra la etiqueta del campo y `otpInput.group` no se lee); las dos siguen como anulación puntual |
| `TimeSelect`, `TimeField` | **Migrado al proveedor** (`timeSelect.hours`, `.minutes`, `.maskHours`, `.maskMinutes`, sin defaults). Las cuatro props (`hoursLabel`, `minutesLabel`, `hoursPlaceholder`, `minutesPlaceholder`) siguen como anulación puntual; `TimeField` reenvía las dos primeras. Las **máscaras** están en el catálogo porque `HH`/`MM` se traducen (`SS`/`MM` en alemán); el dibujo —dos cifras y los dos puntos— no. `label`, `helperText` y `errorMessage` de `TimeField` quedan fuera: son el contenido de ese campo |
| `InputPhone`, `InputPhoneField` | **Migrado al proveedor** (`inputPhone.country`, sin default); `countryLabel` sigue como anulación puntual. `internationalLabel` **conserva su default** (`🌐`): un glifo no se traduce, y meterlo en el catálogo obligaría a repetirlo seis veces |
| `Calendar` | **Migrado al proveedor** (`calendar.previousMonth`, `.nextMonth`, `.previousYears`, `.nextYears`, `.yearGrid`, sin defaults). Las cinco props siguen como anulación puntual. `gridLabel` queda **fuera** del proveedor: nombra a ESE calendario, y sin él la rejilla toma el título del mes. Meses, días, cifras del año y el nombre de cada celda salen de `locale` con `Intl`: no son texto |
| `CalendarPlanner` | **Las dos flechas, migradas** a `calendar.previousMonth` / `.nextMonth` — el mismo espacio que `Calendar`, porque es literalmente el mismo texto. `gridLabel` queda fuera (nombra a ESE planificador); `moreLabel` sigue **sin migrar**, con su castellano puesto |
| `CalendarRoster` | **Las dos flechas, migradas** a `calendar.previousMonth` / `.nextMonth`. `legendLabel`, `nameLabel` y `birthdayPrefix` siguen **sin migrar**; `legendItems` es una lista de datos y se traduce pasándola entera |
| `Table` (`TableHeader`) | **Migrado al proveedor** (`table.*`, sin defaults). Las props `actionsLabel`, `sortedAscLabel`, `sortedDescLabel` y `sortableLabel` siguen existiendo como anulación puntual |
| `Table` (`TableProps`) | `caption` — el nombre de ESTA tabla, sin default y **fuera del proveedor**: no lo puede saber un catálogo común |
| `Table` (`TableRow`) | `label` — el nombre accesible de la fila, sin default: solo lo escribe el consumidor |
| `CodeBlock` | `copyLabel`, `copiedLabel`, `codeLabel` |
| `DescriptionList` (`DescriptionDetails`) | `copyLabel`, `copiedLabel` — solo con `copyable` |
| `CopyableValue` | `copyLabel`, `copiedLabel` |
| `Alert` | `closeLabel` |
| `Banner` | `dismissLabel` |
| `CloseButton` | `label` |
| `Toaster` | `containerAriaLabel`, `closeLabel` |
| `ConversationThread` | `ariaLabel`, `streamingLabel` — más `locale` y `timestampFormat` |
| `ConversationList` | `newLabel`, `navLabel`, `deleteLabel`, `emptyMessage`, `errorTitle` |
| `UserMessage` | `locale`, `timestampFormat` |
| `AssistantMessage` | `streamingLabel` — más `locale` y `timestampFormat` |
| `MessageComposer` | `placeholder`, `sendLabel` — más `helperText`, **sin default en ningún idioma** |
| `ChatShell` | `listLabel`, `listTriggerLabel` |
| `Stepper` | `label` (el nombre de la lista de progreso), `compactLabel` (recibe el paso y el total: «Paso 2 de 4») y `labels` con los estados `completed`, `current` y `pending` |
| `OnboardingShell` | `actionsLabel` — el nombre del grupo de acciones del pie |
| `LoginForm` | `title`, `emailLabel`, `passwordLabel`, `submitLabel`, `loadingLabel` |
| `PasswordField` | **Migrado al proveedor** (`passwordField.show`, `passwordField.hide`, sin defaults). `label`, `placeholder`, `helperText` y `errorMessage` quedan fuera: son el contenido de ese campo |
| `ContextMenu`, `DotsButton` | `label` / `aria-label` |
| `Modal` | `fallbackTitle`, `closeLabel` |
| `Select`, `SelectField` | **Migrado al proveedor** (`select.placeholder`, sin default). La prop `placeholder` sigue como anulación puntual y es la que se usa cuando el marcador dice algo de ESE campo. Las `options` son datos y no pasan por el catálogo |
| `MultiSelect`, `MultiSelectField` | **Migrado al proveedor** (`multiSelect.placeholder`, `multiSelect.remove`, sin defaults). `placeholder` y `removeLabel` siguen como anulación puntual |
| `DatePicker`, `DatePickerField`, `DateTimeField` | **Migrado al proveedor** (`datePicker.openCalendar`, `.invalid`, `.calendar`, `.maskLetters`, sin defaults). Las props siguen como anulación puntual, y `placeholder` sustituye la máscara entera cuando el marcador dice algo de ESE campo. `maskLetters` guarda solo las **letras**: el orden y el separador salen de `locale` (ver § «Una máscara tiene dos mitades»). Los cinco del calendario se siguen reexponiendo con los nombres de `Calendar`, pero **ya no hay que pasarlos para traducir**: el `Calendar` de dentro lee `calendar.*` por contexto; `gridLabel` cae al nombre del panel, y ese cae a la etiqueta del campo. `DateTimeField` añade `hoursLabel` y `minutesLabel`, también reenvíos puros a `timeSelect.*`. `label`, `helperText` y `errorMessage` de los campos quedan fuera: son su contenido |
| `Spinner`, `TypingIndicator` | `label` |
| `NotificationButton` | `label` sin contador y `countLabel` con él — la frase se rehace con el número dentro |
| `NotificationPanel` | los dos de `NotificationButton` (`label`, `countLabel`) más `panelLabel`, `unreadLabel`, `emptyLabel`, `allLabel`, `preferencesLabel` y `markAllReadLabel`. La fecha de cada notificación **no** es prop de texto: llega ya formateada en `items[].time` |
| `ConsentBanner` | `title`, `description`, `policyLabel`, `acceptAllLabel`, `rejectAllLabel`, `preferencesLabel`, `regionLabel` |
| `Carousel` | `label`, `roleDescription`, `trackLabel`, `prevLabel`, `nextLabel`, `indicatorLabel`, `pauseLabel`, `playLabel`, `slideStatusLabel` — y `roleDescription` en `CarouselSlide` |
| `ProjectCard` | `tagsLabel` |
| `Chart` | `ariaLabel`, `tableCaption`, `tableHint`, `categoryLabel`, `valueLabel`, `shareLabel`, `emptyMessage` — más `locale`, `formatValue` y `formatX` |
| `Sparkline` | `ariaLabel` |
| `ConsentPreferences` | `title`, `acceptAllLabel`, `rejectAllLabel`, `closeLabel`, `alwaysOnLabel` — y `categories` para los nombres y las explicaciones de cada categoría |
| `TableOfContents` | `ariaLabel`, `title` |
| `PrevNextNav` | `prevLabel`, `nextLabel` — el `aria-label` de cada control; con `prevTitle`/`nextTitle` pasan a ser el rótulo visible |
| `InputField` | **Migrado al proveedor** (`inputField.clear`, sin default) — el aspa de la variante `kind="search"` con `clearable`; `clearLabel` sigue como anulación puntual. `label` (obligatoria), `placeholder`, `helperText` y `errorMessage` quedan **fuera** del proveedor: son el contenido de ESE campo |
| `DocsSearch` | **Migrado al proveedor** (`docsSearch.label`, `.placeholder`, `.results`, `.empty`, `.loading`, sin defaults). `clearLabel` **no tiene clave propia**: el aspa es la del `InputField` de debajo y sale de `inputField.clear`; la prop sigue como reenvío puro |
| `FilterBar` | **Migrado al proveedor** (`filterBar.label`, sin default) — el nombre del punto de referencia `search`. Los rótulos de los filtros no pasan por el catálogo: los escribe cada campo |
| `SearchForm` | **Migrado al proveedor** (`searchForm.label`, `.placeholder`, `.submit`, sin defaults). Las tres props siguen como anulación puntual |
| `DataTable` | **Migrado al proveedor** (`dataTable.*`, sin defaults): `searchPlaceholder` cae en `dataTable.search` y `emptyMessage` en `dataTable.empty`, y las dos props siguen como anulación puntual. `ariaLabel`/`ariaLabelledBy` (el nombre de la tabla) quedan fuera del proveedor: son de esta pantalla. `searchClearLabel` es un **reenvío puro** al `InputField`, que desde la ola de formularios lee `inputField.clear` por su cuenta: ya no hace falta pasarlo para traducir. `headerLabels` y `paginationLabels` ya no hacen falta para traducir: sus piezas leen del proveedor solas |
| `StatTile` | `delta.label` para leer la dirección de la tendencia |
| `CopyButton` | `label`, `copiedLabel`, `errorLabel` |
| `ConfirmDialog` | `confirmLabel`, `cancelLabel`, `pendingLabel`, `closeLabel` — más `secondaryActionLabel`, el rótulo de la acción intermedia, que es texto del producto y no tiene default, y `confirmPhraseLabel` / `confirmPhraseMismatch`, **obligatorios y sin default en cuanto se pasa `confirmPhrase`** |
| `StarRating` | `valueLabel`, `countLabel`, `emptyLabel`, `optionLabel`, `groupLabel` — **obligatorias y sin default**: el componente recibe su texto, no lo trae puesto. Cada una se exige cuando el componente puede emitirla (`valueLabel` en lectura; `countLabel` con `reviewCount`; `emptyLabel` si `value` puede ser `null`; `optionLabel` y `groupLabel` en entrada). No tiene `locale`: no escribe ningún número |
| `Slider` | `label`, `thumbLabel` — más `format` y `locale` para el número |
| `TreeView` | `label` — el nombre accesible del árbol |
| `SidebarNav` | `label`, `emptyLabel` — la marca de las entradas sin contenido |
| `AnnotationThread` | `label`, `openLabel`, `acknowledgedLabel`, `resolvedLabel`, `editedLabel`, `repliesLabel` — más `locale` y `dateFormat` |
| `ScrollArea` | `label` — el nombre accesible de la región desplazable |
| `ProgressBar` | `label` — qué está avanzando |
| `Breadcrumb` | `ariaLabel` |
| `Sidebar` | `label`, `resizerLabel`, `resizerValueText` — el ancho hablado del asa, con su unidad |
| `AppShell` | `skipLabel` — el enlace de salto al contenido |
| `FloatingDock` | `label` (el nombre del lanzador, sin default), `closeLabel`, `badgeLabel` — cómo se lee el contador de novedades |
| `AppLauncher` | `labels.open` (nombre accesible del disparador cuando es solo icono), `labels.new` (badge de app nueva), `labels.trigger` — texto visible del disparador (opcional: sin él, el disparador se queda como icono solo con `open`), `labels.title` — título del modal en `presentation="modal"` (default «Aplicaciones»; sin uso en `presentation="popover"`) |
| `EmailLayout` | `optOut.manageLabel`, `optOut.unsubscribeLabel`, `optOut.manageBeforeLabel`, `optOut.managePreferencesLabel`, `optOut.manageAfterLabel` — la frase de baja, partida porque los idiomas no coinciden en si hay texto tras el enlace |
| `EmailLayout` (pie sin cuenta) | `optOut.reasonLabel` y `optOut.unsubscribeLabel` (**obligatorias, sin default**) — el motivo por el que le llega el correo a quien no tiene cuenta, y la frase de baja que lo cierra |
| `EmailButton` | `fallbackLabel` (**obligatoria, sin default**) — la frase que presenta el enlace de respaldo |
| `RecoveryCodes` | `labels.list`, `labels.copy`, `labels.copied` (**las tres obligatorias, sin default**, como `AppLauncherLabels`) — nombre accesible de la lista numerada, rótulo del botón de copiar todos y su acuse |

## Al añadir un componente nuevo

Si el componente emite algún texto que no venga de sus `children` ni de sus datos, ese texto va
en una prop — nunca cableado en el JSX. Es la diferencia entre que un consumidor pueda traducirlo
y que tenga que reimplementar el componente entero para hacerlo.

Lo que cambia con el proveedor es **de dónde sale el valor cuando la prop falta**: en un
componente nuevo, declara su interfaz de textos junto a sus props (como `PaginationMessages`),
añádela a `BrandMessages` y lee con `useBrandMessages('<espacio>')` en el punto donde el texto se
pinta. Sin default castellano.

**Al proveedor va solo lo que el componente emite por su cuenta y dice lo mismo en toda la
suite** —«Acciones», «Ordenado ascendente», el aviso de una tabla vacía—: es cromo del
componente. Lo que es contenido de una pantalla concreta —el `caption` de ESA tabla, el vacío
que explica algo del dominio, el nombre de una fila— **se queda como prop y sin default**: un
catálogo común no puede saberlo, y meterlo ahí es peor error que dejarlo fuera. Mientras un componente siga sin migrar, su prop mantiene el
default castellano y su JSDoc lo dice.

### Las excepciones: `EmailButton.fallbackLabel` y el pie de correo sin cuenta

`fallbackLabel` es obligatoria y **sin default castellano**. Dos razones, y las dos apuntan al
mismo sitio: el correo de la suite vive en seis idiomas que conoce `mailer` y no el DS, y un botón
de correo sin su enlace de respaldo debajo es un correo sin plan B. Hacerla obligatoria es lo que
garantiza que ninguna plantilla se lo deje.

Las otras dos son `optOut.reasonLabel` y `optOut.unsubscribeLabel` del **pie de correo sin
cuenta** (`EmailOptOutGuest`), por la misma lógica: el motivo por el que a alguien sin cuenta le
llega un correo lo sabe el producto que lo manda —no el DS—, y siendo el enlace una frase entera
que cierra ese motivo, ningún default podría encajar. Obligatorias, para que ninguna plantilla
mande una baja sin decir de qué.
