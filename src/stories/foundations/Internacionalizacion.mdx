import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Internacionalización" />

# Internacionalización

Los componentes de la librería emiten textos por su cuenta: etiquetas accesibles (`aria-label`,
texto para lectores de pantalla) y, en algunos casos, texto visible en pantalla — un mensaje de
"sin resultados", el sumario de una paginación, la zona de arrastre de un campo de archivos.

Hay **dos formas** de que esos textos lleguen traducidos, y el sistema está migrando de la
primera a la segunda:

1. **Props de texto con default castellano** — la convención histórica, y todavía la de la
   inmensa mayoría de componentes.
2. **El proveedor de textos** (`BrandMessagesProvider`) — el destino: el texto sale del catálogo
   de la aplicación y **no hay default**, así que olvidarlo es un error, no un castellano mudo.
   Hoy lo usan `Pagination` y la **familia de tablas** (`Table`, `DataTable`).

## El proveedor de textos

Se monta **una vez en la raíz de la aplicación**, con el catálogo del idioma vigente:

```tsx
import { BrandMessagesProvider } from '@studiolxd/brand/messages';

<BrandMessagesProvider
  messages={{
    pagination: {
      label: t('pagination.label'),
      pagesGroup: t('pagination.pagesGroup'),
      previous: t('pagination.previous'),
      next: t('pagination.next'),
      goToPage: (page) => t('pagination.goToPage', { page }),
      perPage: t('pagination.perPage'),
      total: (count) => t('pagination.total', { count }),
      allOption: t('pagination.allOption'),
    },
    table: {
      actions: t('table.actions'),
      sortable: t('table.sortable'),
      sortedAscending: t('table.sortedAscending'),
      sortedDescending: t('table.sortedDescending'),
    },
    dataTable: {
      empty: t('dataTable.empty'),
      search: t('common.search.placeholder'),
    },
  }}
>
  {children}
</BrandMessagesProvider>
```

El catálogo va creciendo ola a ola: además de `pagination`, `table` y `dataTable`, hoy están los
espacios de los átomos de formulario —`inputField`, `passwordField`, `select`, `multiSelect`,
`numberInput`, `otpInput`, `inputPhone`, `asyncSelect`, `asyncMultiSelect`, `searchForm`,
`docsSearch` y `filterBar`—, los de fecha y hora —`calendar`, `datePicker` y `timeSelect`—, los
de subida y diálogo —`fileUpload`, `imageCropDialog`, `avatarUpload`, `modal`, `sheet`,
`confirmDialog`, `alert`, `banner` y `toaster`—, los de los envoltorios de diálogo —`consent`,
`commandPalette`, `appLauncher`, `floatingDock`, `notificationButton` y `notificationPanel`—, los
del cromo de aplicación y navegación —`menuButton`, `appRoot`, `appShell`, `sidebar`,
`sidebarNav`, `siteNav`, `siteHeader`, `userMenu`, `orgSwitcher`, `breadcrumb`,
`tableOfContents`, `prevNextNav`, `publicPageShell` y `onboardingShell`— y los de copiar, datos y
estado —`copy`, `codeBlock`, `dotsButton`, `progressBar`, `spinner`, `slider`, `treeView`,
`uptimeBars`, `chart`, `stepper`, `carousel`, `languageSwitcher`, `projectCard`, `legalFooter`,
`calendarRoster`, `calendarPlanner` y `notificationList`—. No hace falta memorizarlos: el tipo `BrandMessages` los lleva todos y
**una app que se deje uno no compila**. La tabla de más abajo dice qué prop cae en qué clave.

A partir de ahí, un componente migrado se monta **sin una sola prop de texto**:

```tsx
<Pagination total={200} page={3} pageSize={10} showTotal onPageChange={setPage} />
```

### Cómo está hecho

- **El tipo nace en el DS.** `BrandMessages` es el contrato, y el catálogo de la app es quien lo
  satisface — nunca al revés. Si el tipo se generase desde el JSON de una suite, el DS dependería
  de un paquete de producto para declarar su propio contrato.
- **Anidado por componente, no plano.** Un espacio por componente (`pagination`, `table`,
  `dataTable`, y los que vengan), calcando los que el catálogo de la suite ya tiene. Son 310 textos: una lista plana de
  claves sueltas no se puede escribir ni revisar.
- **Todas las claves son obligatorias** dentro de su espacio, como en `RecoveryCodesLabels`. Una
  app que se deje una **no compila**: ese es el punto entero del mecanismo.
- **La interfaz de cada espacio vive junto a su componente** (`PaginationMessages` se exporta
  desde `Pagination`), y `BrandMessages` solo las ensambla.

### El orden: prop → proveedor → error

| Escalón | Cuándo |
| --- | --- |
| **La prop** | Cuando se pasa. Es la anulación puntual, para el uso que de verdad no dice lo mismo que todos los demás |
| **El proveedor** | Cuando la prop falta |
| **Un error** | Cuando faltan las dos, con el nombre del texto que falta |

**No hay cuarto escalón.** Un default castellano viaja dentro del paquete y se alcanza en
ejecución: pinta "Página 3" dentro de una interfaz en francés sin que falle nada, sin que lo vea
ningún test, y se ve bien estando mal. El error se lanza **donde el texto se pinta**, así que un
componente no exige el texto de una pieza que no enseña.

### Y el Storybook, ¿no es un default por la puerta de atrás?

No, y la diferencia es de sitio, no de matiz. El Storybook es **la aplicación que monta los
componentes**, así que monta el proveedor como lo hará cada app: un decorador global
(`.storybook/preview.tsx`) lo envuelve todo con un fixture castellano
(`.storybook/brandMessagesFixture.ts`).

Ese fixture **no puede llegar a un consumidor**: vive fuera de `src/`, no es punto de entrada de
la librería, ningún componente lo importa y `package.json#files` solo publica `dist/`,
`src/tokens/` y el `CHANGELOG`. Las cuatro cosas las vigila un test
(`src/messages/BrandMessages.test.ts`), no un comentario que lo prometa. Un default, en cambio,
viaja dentro del `.js` compilado y se alcanza desde cualquier app que olvide un texto.

## La convención histórica (props con default)

Mientras el componente no esté migrado al proveedor, cada texto que emite por su cuenta es una
**prop opcional con el texto castellano como valor por defecto**. Una aplicación multiidioma que
no las pase **pierde sus traducciones**: un usuario de lector de pantalla en inglés o en alemán
oye "Página 3" o ve "Sin resultados" en medio de una interfaz traducida — el fallo silencioso que
el proveedor viene a cerrar.

Cada texto que el componente emite por su cuenta es una **prop opcional con el texto castellano
como valor por defecto**:

```tsx
// sin props → castellano
<Modal open={open} onOpenChange={setOpen} title="Ajustes" />

// con props → los textos del consumidor
<Modal
  open={open}
  onOpenChange={setOpen}
  title={t('settings.title')}
  closeLabel={t('common.close')}
  fallbackTitle={t('common.dialog')}
/>
```

Reglas de la convención:

- **El default es siempre el texto castellano actual**, así que añadir la prop nunca es un
  breaking change ni cambia el marcado de quien no la pasa.
- **Nombre `<cosa>Label`** para etiquetas accesibles y textos cortos; `<cosa>Message` para
  mensajes; `<cosa>Error` para textos de error; `<cosa>Hint`/`<cosa>Placeholder` para pistas.
- **Función en vez de string** cuando el texto interpola un valor:
  `pageLabel?: (page: number) => string`, no un string con marcador.
- **Listas de opciones se traducen pasando la lista entera**, no con una prop de texto por
  elemento: `pageSizeOptions` en `Pagination`, `legendItems` en `CalendarRoster`.
- **El JSDoc de la prop dice cuál es el default y que es castellano**, para que se vea desde el
  autocompletado del editor.

## Fechas y números

Los componentes de calendario no traducen nombres de mes y día con props de texto: reciben una
prop **`locale`** (default `'es-ES'`) y delegan en `Intl.DateTimeFormat`. Las props de texto de
esos componentes cubren solo lo que `Intl` no da — los `aria-label` de navegación, la leyenda.

```tsx
<Calendar locale="de-DE" previousMonthLabel="Vorheriger Monat" nextMonthLabel="Nächster Monat" />
```

```tsx
<Calendar locale="de-DE" />   {/* las flechas salen de `calendar.*` del proveedor */}
```

### Una máscara tiene dos mitades, y no se deciden en el mismo sitio

`dd/mm/aaaa` parece una sola cosa. No lo es, y la campaña del proveedor obliga a separarlas:

- **El orden y el separador son formato.** `25/09/2026` en `es`, `09/25/2026` en `en-US` y
  `25.09.2026` en `de` salen de `Intl.DateTimeFormat().formatToParts()`. No son prop, no están
  en el catálogo y no se cablean por idioma: dependen del `locale`, que es de qué convención de
  escritura se habla, no de en qué idioma está la interfaz. Una app inglesa que enseña fechas
  españolas las quiere en orden español.
- **Las letras son idioma.** `aaaa` es «año», `yyyy` es *year*, `jjjj` es *Jahr*; `HH`/`MM` son
  hora y minuto, y en alemán `SS`/`MM`. Son la abreviatura de una palabra, así que se traducen:
  **son cromo y van al catálogo**, como cualquier otro rótulo.

Por eso el catálogo guarda las **letras sueltas** y nunca la máscara montada — la máscara la
arma el componente poniendo las letras en el orden del locale:

```tsx
// mismo catálogo, dos locales: las letras no se mueven, el orden sí
<DatePicker locale="es-ES" />   {/* dd/mm/yyyy */}
<DatePicker locale="en-US" />   {/* mm/dd/yyyy */}
```

La regla, en una frase: **si cambia al cambiar de idioma, es cromo y va al catálogo; si cambia
al cambiar de país, es formato y va por `locale`.**

### «2,5 MB» tiene dos mitades, y una lista necesita la conjunción del idioma

La misma regla, aplicada a la familia de subidas:

- **El peso.** «máx. 2,5 MB» son dos cosas. La frase es idioma y va al catálogo, **como
  plantilla**; la cifra es formato y la escribe `Intl.NumberFormat` con `style: "unit"` — el
  separador decimal, la agrupación y el espacio antes del símbolo salen del `locale`. El
  símbolo `MB` no se traduce: es una unidad, no una palabra. **Al catálogo no baja nunca un
  «2.5 MB» escrito a mano**, que metería el punto inglés en una interfaz española.
- **La lista de formatos.** «JPEG, PNG o WEBP» no es un `join(", ")` con una «o» pegada: la
  conjunción, la coma de Oxford y el espaciado cambian con la lengua, y la lista acaba dentro
  de una frase traducida. Es `Intl.ListFormat` con `type: "disjunction"`.

```tsx
// mismo catálogo inglés, dos locales
<FileUpload locale="es-ES" maxSize={2621440} />   {/* max. 2,5 MB */}
<FileUpload locale="en-US" maxSize={2621440} />   {/* max. 2.5 MB */}
```

## Componentes con props de texto

| Componente | Props |
| --- | --- |
| `Pagination` | **Migrado al proveedor** (`pagination.*`, sin defaults). Las props `ariaLabel`, `pageLabel`, `previousLabel`, `nextLabel`, `pagesGroupLabel`, `pageSizeLabel` y `totalLabel` siguen existiendo como anulación puntual; `pageSizeOptions` sustituye la lista entera del selector |
| `AsyncSelect`, `AsyncSelectField` | **Migrado al proveedor** (`asyncSelect.*`, sin defaults). `placeholder`, `emptyMessage`, `loadingLabel` y `clearLabel` siguen como anulación puntual; `AsyncSelectField` las reenvía |
| `AsyncMultiSelect`, `AsyncMultiSelectField` | **Migrado al proveedor** (`asyncMultiSelect.*`, sin defaults). `placeholder`, `emptyMessage`, `loadingLabel` y `removeLabel` siguen como anulación puntual; `AsyncMultiSelectField` las reenvía |
| `FileUpload`, `FileUploadField` | **Migrado al proveedor** (`fileUpload.dropzone`, `.dropzoneActive`, `.dropzoneHint`, `.maxSize`, `.maxFiles`, `.files`, `.progress`, `.removeFile`, `.tooLarge`, `.invalidType`, sin defaults). Las diez props siguen como anulación puntual. Las dos pistas de límite y el error de peso son **plantillas** que reciben el peso **ya escrito por `Intl.NumberFormat`** con el `locale`: la frase es idioma, la cifra es formato (ver § «"2,5 MB" tiene dos mitades»). `FileUploadField` no emite texto propio: reenvía todo por contexto, y `label`, `helperText` y `errorMessage` son su contenido |
| `AvatarUpload` | **Migrado al proveedor** (`avatarUpload.button`, `.buttonFor`, `.subject`, `.dropHint`, `.dropActive`, `.maxSize`, `.invalidType`, `.tooLarge`, `.cropCancel`, `.cropConfirm`, sin defaults). **El par visible/accesible del botón va entero en el catálogo**: WCAG 2.5.3 exige que el nombre accesible contenga el visible, y partirlo entre el catálogo común y el de la pantalla dejaría los dos lados del contrato en ficheros distintos. `buttonFor` es una **plantilla** —el orden del verbo y del sujeto es del idioma— y el catálogo lleva la regla escrita: `buttonFor(x)` contiene `button`. Fuera del catálogo quedan `subject` (qué se sube: es de la pantalla) y `cropTitle` (obligatorio y sin default, como el `title` del `ImageCropDialog`). `cropCloseLabel`, `cropLoadingLabel` y `cropErrorMessage` son reenvíos puros al diálogo — y `cropCloseLabel` llega hasta el `Modal`, que ya lee `modal.close` solo |
| `ImageCropDialog` | **Migrado al proveedor** (`imageCropDialog.loading`, `.error`, sin defaults) — `title`, `cancelLabel` y `confirmLabel` siguen siendo obligatorias y sin default, y **no** van al catálogo: el diálogo no sabe qué se está recortando ni qué pasa al confirmar, y eso es contenido de la pantalla. Es el ejemplo canónico de la línea cromo/contenido. `closeLabel` es reenvío puro al `Modal`, que desde la ola de diálogos lee `modal.close` por su cuenta: ya no hace falta pasarlo para traducir |
| `NumberInput`, `NumberInputField` | **Migrado al proveedor** (`numberInput.decrement`, `numberInput.increment`, sin defaults). Las props siguen como anulación puntual |
| `OtpInput`, `OtpField` | **Migrado al proveedor** (`otpInput.digit`, `otpInput.group`, sin defaults). `digitLabel` nombra cada celda y `groupLabel` el grupo del control suelto (dentro de `OtpField` lo nombra la etiqueta del campo y `otpInput.group` no se lee); las dos siguen como anulación puntual |
| `TimeSelect`, `TimeField` | **Migrado al proveedor** (`timeSelect.hours`, `.minutes`, `.maskHours`, `.maskMinutes`, sin defaults). Las cuatro props (`hoursLabel`, `minutesLabel`, `hoursPlaceholder`, `minutesPlaceholder`) siguen como anulación puntual; `TimeField` reenvía las dos primeras. Las **máscaras** están en el catálogo porque `HH`/`MM` se traducen (`SS`/`MM` en alemán); el dibujo —dos cifras y los dos puntos— no. `label`, `helperText` y `errorMessage` de `TimeField` quedan fuera: son el contenido de ese campo |
| `InputPhone`, `InputPhoneField` | **Migrado al proveedor** (`inputPhone.country`, sin default); `countryLabel` sigue como anulación puntual. `internationalLabel` **conserva su default** (`🌐`): un glifo no se traduce, y meterlo en el catálogo obligaría a repetirlo seis veces |
| `Calendar` | **Migrado al proveedor** (`calendar.previousMonth`, `.nextMonth`, `.previousYears`, `.nextYears`, `.yearGrid`, sin defaults). Las cinco props siguen como anulación puntual. `gridLabel` queda **fuera** del proveedor: nombra a ESE calendario, y sin él la rejilla toma el título del mes. Meses, días, cifras del año y el nombre de cada celda salen de `locale` con `Intl`: no son texto |
| `CalendarPlanner` | **Migrado al proveedor** (`calendarPlanner.more`, sin default) — el botón de desbordamiento, que interpola la cuenta y por eso es función. Las dos flechas siguen en `calendar.previousMonth` / `.nextMonth`, el mismo espacio que `Calendar`, porque es literalmente el mismo texto. `gridLabel` queda fuera: nombra a ESE planificador. Los cinco textos de la **vista de semana** (`previousWeekLabel`, `nextWeekLabel`, `monthViewLabel`, `weekViewLabel`, `viewSwitcherLabel`) van todavía en props con default castellano: sumarlos al catálogo como claves obligatorias dejaría sin compilar a quien ya lo tiene montado, y la semana llegó en un minor — pasan al catálogo en el próximo major |
| `CalendarRoster` | **Migrado al proveedor** (`calendarRoster.name`, `.legend` y los seis tipos `.holiday`, `.vacation`, `.absence`, `.recovery`, `.birthday`, `.nonWorking`, sin defaults); las dos flechas siguen en `calendar.previousMonth` / `.nextMonth`. **Los seis tipos van clave a clave y no como lista**, contra la regla general: es un vocabulario **cerrado** del componente, y montar el array con sus `type` correctos solo para traducir seis palabras era peor. `legendItems` sigue sustituyendo la leyenda **entera** —otro orden, otros tipos— y gana. `birthdayPrefix` queda fuera: es un emoji, y un emoji no se traduce |
| `Table` (`TableHeader`) | **Migrado al proveedor** (`table.*`, sin defaults). Las props `actionsLabel`, `sortedAscLabel`, `sortedDescLabel` y `sortableLabel` siguen existiendo como anulación puntual |
| `Table` (`TableProps`) | `caption` — el nombre de ESTA tabla, sin default y **fuera del proveedor**: no lo puede saber un catálogo común |
| `Table` (`TableRow`) | `label` — el nombre accesible de la fila, sin default: solo lo escribe el consumidor |
| `CodeBlock` | **Migrado al proveedor** (`codeBlock.copy`, `codeBlock.region`, sin defaults) — y el acuse sale de **`copy.copied`**, el espacio de la familia: el bloque lee de dos espacios porque su rótulo dice «Copiar código» (suyo) pero su acuse dice «Copiado» (de todos). `region` interpola el lenguaje, así que es función. Las tres props siguen como anulación puntual |
| `DescriptionList` (`DescriptionDetails`) | **No tiene texto propio.** Con `copyable` monta un `CopyableValue`, que lee `copy.label` y `copy.copied`; `copyLabel` y `copiedLabel` son **reenvíos puros** y ya no hacen falta para traducir |
| `CopyableValue` | **Migrado al proveedor** (`copy.label`, `copy.copied`, sin defaults) — el **mismo espacio** que `CopyButton`: son literalmente las mismas dos palabras. `copyText` no es texto de interfaz, es el dato que se copia |
| `Alert` | **Migrado al proveedor** (`alert.close`, sin default). `closeLabel` sigue como anulación puntual, y solo se lee cuando hay `dismissible`. `title`, `description`, `children` y lo que digan las `actions` quedan fuera: el aviso cuenta qué ha pasado en ESA pantalla |
| `Banner` | **Migrado al proveedor** (`banner.dismiss`, sin default). `dismissLabel` sigue como anulación puntual, y solo se lee cuando hay `onDismiss`. Lo que la barra cuenta y lo que ofrezca su acción son contenido de la aplicación |
| `CloseButton` | `label` |
| `Toaster` | **Migrado al proveedor** (`toaster.container`, `toaster.close`, sin defaults). Las dos props siguen como anulación puntual; `toaster.close` solo se lee con `closeButton`. El título, la `description` y el `label` de la `action` de cada `toast(...)` los pone quien lanza el aviso — el `label` nunca tuvo default. **El `Toaster` se monta dentro del `BrandMessagesProvider`** |
| `ConversationThread` | **Migrado al proveedor** (`conversationThread.label`, sin default); `ariaLabel` sigue como anulación puntual. El estado de escritura **no es suyo**: lo pinta el `TypingIndicator` (`typingIndicator.typing`) y el hilo solo lo reenvía. `streamingName` es **obligatoria y sin default** —el nombre del asistente es contenido—, y `streamingLabel` sustituye la frase entera. `locale` y `timestampFormat` son formato |
| `ConversationList` | **Migrado al proveedor** (`conversationList.new`, `.nav`, `.delete`, `.empty`, `.error`, sin defaults). Las cinco props siguen como anulación puntual, y cada texto se lee **donde se pinta**: una lista que carga bien no exige el título del aviso de error. `delete` interpola el título, así que es función. El **mensaje** de `error`, los títulos de `conversations` y `emptyDescription` quedan fuera: son datos de la pantalla — `emptyDescription` nunca tuvo clave de catálogo |
| `UserMessage` | `locale`, `timestampFormat` |
| `AssistantMessage` | **No tiene texto propio.** Lo único que emite por su cuenta es el estado de escritura, que pinta el `TypingIndicator` con `typingIndicator.typing(name)`. `streamingName` es **obligatoria y sin default** y se pide siempre, aunque el mensaje no esté generándose: un nombre que aparece solo a veces es un texto sin traducir esperando su turno. `model` es dato; `locale` y `timestampFormat`, formato |
| `MessageComposer` | **Migrado al proveedor** (`messageComposer.placeholder`, `messageComposer.send`, sin defaults). Las dos props siguen como anulación puntual. `helperText` **sigue fuera y sin default en ningún idioma**: la línea de ayuda cuenta el atajo de teclado o un aviso de privacidad de ESE chat. `inputLabel` tampoco entra: nombra ese campo |
| `ChatShell` | **Migrado al proveedor** (`chatShell.list`, `chatShell.listTrigger`, sin defaults). **Espacio propio y no el de `ConversationList`**, aunque hoy digan la misma palabra: son dos piezas distintas, y el precedente es `appRoot.skipToContent` frente a `appShell.skipToContent`. Sin `list` no se lee ninguna de las dos |
| `Stepper` | **Migrado al proveedor** (`stepper.label`, `.compact`, `.completed`, `.current`, `.pending`, sin defaults). `compact` interpola el paso y el total, así que es función. `labels` sigue siendo anulación **clave a clave**, y cada marca se lee donde se pinta: un progreso que empieza en el primer paso no exige `completed` |
| `OnboardingShell` | **Migrado al proveedor** (`onboardingShell.actions`, sin default). `actionsLabel` sigue como anulación puntual y solo se lee cuando el paso trae alguna acción. `preferencesLabel` es un **reenvío puro** al `PublicPageShell`. Lo que digan «Continuar», «Atrás» y la salida es contenido del flujo |
| `LoginForm` | `title`, `emailLabel`, `passwordLabel`, `submitLabel`, `loadingLabel` |
| `PasswordField` | **Migrado al proveedor** (`passwordField.show`, `passwordField.hide`, sin defaults). `label`, `placeholder`, `helperText` y `errorMessage` quedan fuera: son el contenido de ese campo |
| `DotsButton` | **Migrado al proveedor** (`dotsButton.label`, sin default); `aria-label` sigue como anulación puntual |
| `ContextMenu` | **No tiene texto propio.** El nombre del disparador lo pone el `DotsButton`; `label` es un **reenvío puro** y ya no hace falta para traducir |
| `Sheet` | **Migrado al proveedor** (`sheet.close`, sin default). `closeLabel` sigue como anulación puntual y solo se lee cuando el aspa se pinta (sin `hideClose`). `title` —obligatorio—, `description` y el `footer` son contenido |
| `Modal` | **Migrado al proveedor** (`modal.close`, `modal.fallbackTitle`, sin defaults). Las dos props siguen como anulación puntual; `modal.fallbackTitle` solo se lee cuando NO hay `title`. Todos los diálogos construidos sobre él (`ConfirmDialog`, `ImageCropDialog`, `AvatarUpload`, `CommandPalette`, `Consent`) reenvían su `closeLabel` **sin repetir la clave** |
| `CommandPalette` | **Migrado al proveedor** (`commandPalette.title`, `.placeholder`, `.empty`, `.list`, sin defaults) — antes eran props **obligatorias** en cada call-site. Las cuatro siguen como anulación puntual. `closeLabel` es un reenvío puro al `Modal`. Los grupos y los comandos son contenido y viajan en `groups` |
| `Select`, `SelectField` | **Migrado al proveedor** (`select.placeholder`, sin default). La prop `placeholder` sigue como anulación puntual y es la que se usa cuando el marcador dice algo de ESE campo. Las `options` son datos y no pasan por el catálogo |
| `MultiSelect`, `MultiSelectField` | **Migrado al proveedor** (`multiSelect.placeholder`, `multiSelect.remove`, sin defaults). `placeholder` y `removeLabel` siguen como anulación puntual |
| `DatePicker`, `DatePickerField`, `DateTimeField` | **Migrado al proveedor** (`datePicker.openCalendar`, `.invalid`, `.calendar`, `.maskLetters`, sin defaults). Las props siguen como anulación puntual, y `placeholder` sustituye la máscara entera cuando el marcador dice algo de ESE campo. `maskLetters` guarda solo las **letras**: el orden y el separador salen de `locale` (ver § «Una máscara tiene dos mitades»). Los cinco del calendario se siguen reexponiendo con los nombres de `Calendar`, pero **ya no hay que pasarlos para traducir**: el `Calendar` de dentro lee `calendar.*` por contexto; `gridLabel` cae al nombre del panel, y ese cae a la etiqueta del campo. `DateTimeField` añade `hoursLabel` y `minutesLabel`, también reenvíos puros a `timeSelect.*`. `label`, `helperText` y `errorMessage` de los campos quedan fuera: son su contenido |
| `Spinner` | **Migrado al proveedor** (`spinner.label`, sin default). **No se lee con `aria-hidden`**: un spinner decorativo no anuncia nada. El texto **conserva los puntos suspensivos** (ver [Redacción](?path=/docs/foundations-redaccion--docs) § «Los estados de carga llevan puntos suspensivos») |
| `TypingIndicator` | **Migrado al proveedor** (`typingIndicator.typing(name)`, sin default) — y es el caso que enseña la **partición de una frase**: que alguien esté escribiendo es cromo (el verbo y el orden son del idioma), pero **quién** escribe es contenido, así que `name` es **obligatoria y sin default**. La clave es función porque interpola el nombre; `label` sustituye la frase entera y sigue siendo anulación puntual |
| `NotificationButton` | **Migrado al proveedor** (`notificationButton.label`, `.countLabel`, sin defaults). Las dos props siguen como anulación puntual, y cada una se lee **donde se pinta**: sin contador no se exige `countLabel`, con contador no se exige `label`. La frase con número es una función, para que la cifra vaya donde el idioma la coloque |
| `NotificationPanel` | **Migrado al proveedor** (`notificationPanel.panel`, `.unread`, `.empty`, `.all`, `.preferences`, `.markAllRead`, sin defaults). Las seis props siguen como anulación puntual; `markAllRead` solo se lee cuando el botón se pinta. `label` y `countLabel` son **reenvíos puros** al `NotificationButton`, que lee `notificationButton.*` por su cuenta: el panel no repite la clave. La fecha de cada notificación **no** es prop de texto: llega ya formateada en `items[].time` |
| `ConsentBanner` | **Migrado al proveedor** (`consent.title`, `.regionLabel`, `.acceptAll`, `.rejectAll`, `.preferences`, sin defaults) — los botones nombran las tres salidas que pide la ePrivacy y son las mismas en todos los sitios. **`description` y `policyLabel` quedan fuera y son obligatorias**: el texto legal depende de la jurisdicción y de lo que el producto guarde de verdad, y el tipo exige `policyLabel` en cuanto hay `policyHref` — o el enlace no está, o está entero |
| `Carousel` | **Migrado al proveedor** (`carousel.label`, `.roleDescription`, `.track`, `.previous`, `.next`, `.indicator`, `.pause`, `.play`, `.slideStatus`, `.slideRoleDescription`, sin defaults). Las dos `aria-roledescription` **son texto** y no una palabra clave del motor: las lee el lector de pantalla en voz alta. Cada clave se lee donde se pinta: sin `controls` no se exigen las flechas, sin `indicators` el indicador, sin `autoplay` los dos del mando |
| `ProjectCard` | **Migrado al proveedor** (`projectCard.tags`, sin default), y solo se lee cuando hay etiquetas. El título, la descripción y los rótulos de las etiquetas son contenido |
| `Chart` | **Migrado al proveedor** (`chart.tableCaption`, `.tableHint`, `.category`, `.value`, `.share`, `.empty`, sin defaults); `value` y `share` solo se leen en `pie`/`donut`, y `empty` solo con la serie vacía. **`ariaLabel` sigue obligatorio y fuera del catálogo**: dice qué cuenta ESTE gráfico, y un texto común lo dejaría diciendo «Gráfico» en todas partes. `locale`, `formatValue` y `formatX` son formato |
| `Sparkline` | `ariaLabel` |
| `ConsentPreferences` | **Migrado al proveedor** (`consent.preferencesTitle`, `consent.alwaysOn`, sin defaults) — el mismo espacio que la banda, porque es la misma pieza. `closeLabel` es un reenvío puro al `Modal`/`Sheet` (`modal.close` / `sheet.close`). Los nombres y las explicaciones de cada categoría son contenido y se traducen pasando `categories` entera |
| `TableOfContents` | **Migrado al proveedor** (`tableOfContents.label`, sin default). `ariaLabel` sigue como anulación puntual. `title` queda **fuera**: es el rótulo visible de ESE índice, y hay páginas que no lo ponen |
| `PrevNextNav` | **Migrado al proveedor** (`prevNextNav.previous`, `.next`, sin defaults) — dicen la **dirección**, no el destino. Las dos props siguen como anulación puntual, y ahí está el caso de un par que navega semanas y quiere decir «Semana anterior». `prevTitle`, `nextTitle` y `label` quedan fuera: son de esta pantalla |
| `InputField` | **Migrado al proveedor** (`inputField.clear`, sin default) — el aspa de la variante `kind="search"` con `clearable`; `clearLabel` sigue como anulación puntual. `label` (obligatoria), `placeholder`, `helperText` y `errorMessage` quedan **fuera** del proveedor: son el contenido de ESE campo |
| `DocsSearch` | **Migrado al proveedor** (`docsSearch.label`, `.placeholder`, `.results`, `.empty`, `.loading`, sin defaults). `clearLabel` **no tiene clave propia**: el aspa es la del `InputField` de debajo y sale de `inputField.clear`; la prop sigue como reenvío puro |
| `FilterBar` | **Migrado al proveedor** (`filterBar.label`, sin default) — el nombre del punto de referencia `search`. Los rótulos de los filtros no pasan por el catálogo: los escribe cada campo |
| `SearchForm` | **Migrado al proveedor** (`searchForm.label`, `.placeholder`, `.submit`, sin defaults). Las tres props siguen como anulación puntual |
| `DataTable` | **Migrado al proveedor** (`dataTable.*`, sin defaults): `searchPlaceholder` cae en `dataTable.search` y `emptyMessage` en `dataTable.empty`, y las dos props siguen como anulación puntual. `ariaLabel`/`ariaLabelledBy` (el nombre de la tabla) quedan fuera del proveedor: son de esta pantalla, igual que `emptyDescription` (segunda frase del estado vacío, sin clave de catálogo). `searchClearLabel` es un **reenvío puro** al `InputField`, que desde la ola de formularios lee `inputField.clear` por su cuenta: ya no hace falta pasarlo para traducir. `headerLabels` y `paginationLabels` ya no hacen falta para traducir: sus piezas leen del proveedor solas |
| `StatTile` | `delta.label` para leer la dirección de la tendencia |
| `CopyButton` | **Migrado al proveedor** (`copy.label`, `.copied`, `.error`, sin defaults). **`copy` es un espacio de conducta y no de componente**, la primera excepción a «un espacio por componente»: las mismas tres palabras las dicen `CopyableValue`, `DescriptionDetails copyable` y el acuse de `CodeBlock`, y el catálogo de la suite también las tiene una sola vez (`common.copy`, `common.copied`). `copy.label` solo se lee sin rótulo visible; `copy.error`, solo cuando el portapapeles falla |
| `ConfirmDialog` | **Migrado al proveedor** (`confirmDialog.cancel`, `confirmDialog.pending`, sin defaults). **`confirmLabel` pasa a ser obligatoria y sin default, y NO va al catálogo**: es donde se toma la decisión y tiene que nombrar la consecuencia («Borrar la organización»); un `common.confirm` la dejaría diciendo «Confirmar» en todas partes sin que nada fallara. `cancelLabel` sí es cromo —«Cancelar» describe lo único que hace ese botón, no seguir— y `pendingLabel` también, porque es un **estado** sobre un botón ya deshabilitado y nadie decide nada leyéndolo. `closeLabel` es reenvío puro al `Modal`. `secondaryActionLabel`, `confirmPhraseLabel` y `confirmPhraseMismatch` siguen siendo texto del producto, sin default |
| `StarRating` | `valueLabel`, `countLabel`, `emptyLabel`, `optionLabel`, `groupLabel` — **obligatorias y sin default**: el componente recibe su texto, no lo trae puesto. Cada una se exige cuando el componente puede emitirla (`valueLabel` en lectura; `countLabel` con `reviewCount`; `emptyLabel` si `value` puede ser `null`; `optionLabel` y `groupLabel` en entrada). No tiene `locale`: no escribe ningún número |
| `Slider` | **Migrado al proveedor** (`slider.value`, `.min`, `.max`, `.valueAt`, sin defaults), y cada clave se lee donde se pinta: un deslizador de un pulgar no exige las del rango. `label` dice qué mide y gana sobre `slider.value`; `thumbLabel` sustituye el nombre de todos los pulgares. `format` y `locale` son formato |
| `TreeView` | **Migrado al proveedor** (`treeView.label`, sin default) — el nombre **genérico**; el de ESE árbol sigue en la prop, que gana. Los rótulos de los nodos son datos |
| `SidebarNav` | **Migrado al proveedor** (`sidebarNav.label`, `.empty`, sin defaults). Las dos props siguen como anulación puntual; `sidebarNav.empty` solo se lee cuando hay alguna entrada `empty`. Los rótulos de `entries` son datos |
| `AnnotationThread` | **Migrado al proveedor** (`annotationThread.label`, `.open`, `.acknowledged`, `.resolved`, `.edited`, `.replies`, sin defaults). **Los tres estados van clave a clave y no como lista**, contra la regla general: es un vocabulario **cerrado** del componente —abierta, atendida, resuelta—, el mismo caso que los seis tipos de `CalendarRoster`. Las props siguen ganando, así que una revisión puede llamar «Verificada» a `acknowledged`. `replies` lleva el plural, así que es función. Cada texto se lee donde se pinta: un hilo abierto no exige los otros dos rótulos. `locale` y `dateFormat` son formato |
| `ScrollArea` | `label` — el nombre accesible de la región desplazable |
| `ProgressBar` | **Migrado al proveedor** (`progressBar.label`, sin default) — el nombre **genérico** («Progreso»); qué avanza de verdad en ESA pantalla («Subiendo el vídeo») sigue en la prop, que gana. La cifra es un número, no texto |
| `Breadcrumb` | **Migrado al proveedor** (`breadcrumb.label`, sin default). `ariaLabel` sigue como anulación puntual. Los rótulos del rastro son datos, y `separator` es un glifo, no un texto |
| `Sidebar` | **Migrado al proveedor** (`sidebar.label`, `.resizer`, `.resizerValue`, sin defaults). `resizerValue` **interpola el ancho**, así que es una función. Los dos textos del asa solo se leen cuando el asa existe: en escritorio, dentro de un `AppShell` y con la barra no cerrada |
| `AppShell` | **Migrado al proveedor** (`appShell.skipToContent`, sin default). `skipLabel` sigue como anulación puntual. Lo demás son reenvíos: el nombre de la barra lo lee el `Sidebar` y el del botón, el `MenuButton` |
| `AppRoot` | **Migrado al proveedor** (`appRoot.skipToContent`, sin default). Es la misma frase que la del `AppShell` pero **espacio propio**: son dos piezas distintas —la raíz del documento y el armazón de la aplicación— y cada una declara lo suyo, aunque la app las alimente del mismo `t()`. `skipHref` no es texto |
| `MenuButton` | **Migrado al proveedor** (`menuButton.open`, `.close`, sin defaults). **El nombre sigue al estado**: abierto dice «cerrar», cerrado dice «abrir» — antes se quedaba en el mismo nombre salvo que el consumidor pasara `closeLabel` a mano. `menuButton.close` solo se lee cuando el menú se abre |
| `AppHeader` | **No tiene texto propio.** `menuLabel` y `menuCloseLabel` son **reenvíos puros** al `MenuButton` y ya no hacen falta para traducir |
| `SiteHeader` | **Migrado al proveedor** (`siteHeader.logo`, sin default). Ojo con el valor: el default retirado llevaba la marca dentro («Studio LXD — ir al inicio»), así que cada producto escribe la clave con la suya. `menuLabel`/`menuCloseLabel` son reenvíos puros al `MenuButton` |
| `SiteNav` | **Migrado al proveedor** (`siteNav.label`, sin default). Los grupos y sus enlaces son el índice de ESE sitio, y el `badge` de cada ítem («Próximamente», «Beta») llega ya traducido con ellos |
| `UserMenu` | **Migrado al proveedor** (`userMenu.trigger`, `.unread`, sin defaults). Las dos **interpolan un dato**, así que son funciones. `unread` ni siquiera era una prop antes: el contador traía el castellano cableado. `name`, `email` e `items` son contenido |
| `OrgSwitcher` | **Migrado al proveedor** (`orgSwitcher.trigger`, sin default), que interpola la organización activa. Los nombres de las organizaciones son datos |
| `PublicPageShell` | **Migrado al proveedor** (`publicPageShell.preferences`, sin default) — el nombre de la banda de idioma y tema. Solo se lee cuando hay banda |
| `Switcher` | **No emite ningún texto.** Lo nombra la etiqueta de su campo (`SwitcherField`) |
| `FloatingDock` | **Migrado al proveedor, con espacio propio** (`floatingDock.close`, `floatingDock.badge`, sin defaults). **No hereda `modal.close`, a propósito**: su panel no es un `Modal` —es un diálogo no modal, sin velo, que no atrapa el foco, montado por portal dentro del propio ancla y anclado a una esquina—, así que hacerlo pasar por `Modal` habría sido cambiar el componente, no quitar una clave. `label` (el nombre del lanzador) sigue **obligatorio y fuera del catálogo**: nombra lo que el dock abre, y eso lo sabe el producto |
| `AppLauncher` | **Migrado al proveedor** (`appLauncher.open`, `.new`, `.title`, sin defaults). `labels` **deja de ser obligatoria** y queda como anulación puntual, clave a clave. `labels.trigger` **no va al catálogo**: no es un texto de más, su presencia decide la cara del disparador (solo icono, o icono con rótulo), y un texto de catálogo está siempre presente — es una decisión de maqueta del sitio donde se monta. Los nombres de las apps viajan en `apps`, y con ellos el `badge` de cada una («Próximamente», «Beta»), ya traducido — `isNew` es la excepción y por eso su texto sí es cromo: no trae texto, solo enciende el del catálogo |
| `EmailLayout` | `optOut.manageLabel`, `optOut.unsubscribeLabel`, `optOut.manageBeforeLabel`, `optOut.managePreferencesLabel`, `optOut.manageAfterLabel` — la frase de baja, partida porque los idiomas no coinciden en si hay texto tras el enlace |
| `EmailLayout` (pie sin cuenta) | `optOut.reasonLabel` y `optOut.unsubscribeLabel` (**obligatorias, sin default**) — el motivo por el que le llega el correo a quien no tiene cuenta, y la frase de baja que lo cierra |
| `EmailButton` | `fallbackLabel` (**obligatoria, sin default**) — la frase que presenta el enlace de respaldo |
| `LanguageSwitcher` | **Migrado al proveedor** (`languageSwitcher.label`, sin default) — la etiqueta del control. **Los nombres de los idiomas no van al catálogo, y no es olvido**: cada uno va en su propio idioma, precisamente para que se reconozca sin entender la interfaz, y viajan en `languages` |
| `LegalFooter` | **Migrado al proveedor** (`legalFooter.label`, sin default) — el nombre del `nav`. Los rótulos de los enlaces son datos y el `title` es el contenido de ESE pie |
| `NotificationList` | **Migrado al proveedor** (`notificationList.label`, `.unread`, `.markRead`, sin defaults). **Espacio propio y no el del `NotificationPanel`**: son dos piezas distintas —la bandeja a página completa y el flotante de la cabecera— y el catálogo de la suite también las tiene aparte. `markRead` solo se lee con `onMarkRead` y alguna fila sin leer; la hora llega ya formateada en `items[].time` |
| `UptimeBars` | **Migrado al proveedor** (`uptimeBars.label`, `uptimeBars.noData`, sin defaults). Las fechas y la media llegan **ya escritas** en `points[].label` y `summary` —la tira no sabe de tiempo— y el porcentaje lo formatea `Intl`. `pointLabel` sigue siendo prop: el orden de sus piezas es de quien lo monta |
| `RecoveryCodes` | `labels.list`, `labels.copy`, `labels.copied` (**las tres obligatorias, sin default**) — nombre accesible de la lista numerada, rótulo del botón de copiar todos y su acuse. **No caen en `copy.*`**: «Copiar todos» no es «Copiar», y el botón lleva rótulo visible, así que `copy.label` ni se lee. Lo que sí sale del catálogo es el aviso de portapapeles roto (`copy.error`), que este componente no reexpone |
| `UntrustedText` | **Migrado al proveedor** (`untrustedText.expand`, `.collapse`, `.quotes`, sin defaults). Las **comillas** están en el catálogo y no cableadas porque son puntuación del idioma (`« »` en castellano, `“ ”` en inglés, `„ “` en alemán). El valor de fuera entra por `value` y no pasa por el catálogo: es dato, y por eso se pinta entrecomillado, aislado y con los invisibles a la vista |
| `ConnectorRequestSummary` | **Migrado al proveedor** los cinco rótulos (`connectorRequestSummary.client`, `.product`, `.account`, `.scope`, `.redirect`, sin defaults): nombran **qué dato** va en cada fila, que es el vocabulario cerrado de una petición de OAuth. **`scopeReadLabel` y `scopeWriteLabel` pasan a ser obligatorias y sin default, y NO van al catálogo**: afirman qué va a poder hacer la herramienta con los datos de quien lee la pantalla, y eso lo sabe el servidor de autorización. `expandLabel`, `collapseLabel` y `valueQuotes` son reenvíos puros a `UntrustedText` |
| `ConnectorConsentPage` | **Migrado al proveedor** (`connectorConsent.title`, `connectorConsent.deny`, sin defaults). **`intro`, `redirectNotice`, `approveLabel`, `scopeReadLabel` y `scopeWriteLabel` pasan a ser obligatorias y sin default**: la frase cuenta qué se consiente, el aviso afirma a dónde sale el acceso y `approveLabel` es el botón que concede — mismo caso y misma respuesta que `ConfirmDialog.confirmLabel`. `denyLabel` sí es cromo, como «Cancelar»: describe lo único que hace, no seguir |
| `ConnectorSignInPage` | **Migrado al proveedor** (`connectorSignIn.title`, `.signIn`, `.fallbackProduct`, sin defaults) — aquí **no se concede nada**, así que el título y la acción sí son cromo. **`intro`, `scopeReadLabel` y `scopeWriteLabel` pasan a ser obligatorias y sin default**: quien va a teclear una contraseña tiene derecho a saber qué herramienta hay detrás y qué se le va a pedir |
| `ConnectorExternalSignInPage` | **Migrado al proveedor** (`connectorExternalSignIn.title`, `.organization`, `.submit(platform)`, sin defaults). `submit` es **función** por lo mismo que `typingIndicator.typing`: el verbo es cromo y el nombre de la instalación es contenido. **`platformName`, `intro` y `signingInTo` pasan a ser obligatorias y sin default** — la primera nombra al producto, las otras dos afirman qué se autoriza y a dónde va la sesión. `platformName` es ahora `string`, no `ReactNode`, porque el catálogo la interpola |
| `ConnectorRejectionPage` | **Migrado al proveedor** su único texto de cromo (`connectorRejection.code`, sin default). **`title`, `description`, `hint` y `retryLabel` pasan a ser obligatorias y sin default, y la prop `reason` desaparece**: los veinte textos de los cinco motivos vivían en un `Record` dentro del componente, y cada uno afirma algo sobre una comprobación de seguridad. El tipo `ConnectorRejectionReason` se queda como **vocabulario** —la lista de los cinco, escrita en el sistema— para que la aplicación teclee contra él al sacar los cuatro textos de su catálogo |
| `ConnectorAuthShell` | **No tiene texto propio.** `preferencesLabel` es un **reenvío puro** al `PublicPageShell`, que lee `publicPageShell.preferences` por su cuenta |

## Al añadir un componente nuevo

Si el componente emite algún texto que no venga de sus `children` ni de sus datos, ese texto va
en una prop — nunca cableado en el JSX. Es la diferencia entre que un consumidor pueda traducirlo
y que tenga que reimplementar el componente entero para hacerlo.

Lo que cambia con el proveedor es **de dónde sale el valor cuando la prop falta**: en un
componente nuevo, declara su interfaz de textos junto a sus props (como `PaginationMessages`),
añádela a `BrandMessages` y lee con `useBrandMessages('<espacio>')` en el punto donde el texto se
pinta. Sin default castellano.

**Al proveedor va solo lo que el componente emite por su cuenta y dice lo mismo en toda la
suite** —«Acciones», «Ordenado ascendente», el aviso de una tabla vacía—: es cromo del
componente. Lo que es contenido de una pantalla concreta —el `caption` de ESA tabla, el vacío
que explica algo del dominio, el nombre de una fila— **se queda como prop y sin default**: un
catálogo común no puede saberlo, y meterlo ahí es peor error que dejarlo fuera. Mientras un componente siga sin migrar, su prop mantiene el
default castellano y su JSDoc lo dice.

### Las excepciones: `EmailButton.fallbackLabel` y el pie de correo sin cuenta

`fallbackLabel` es obligatoria y **sin default castellano**. Dos razones, y las dos apuntan al
mismo sitio: el correo de la suite vive en seis idiomas que conoce `mailer` y no el DS, y un botón
de correo sin su enlace de respaldo debajo es un correo sin plan B. Hacerla obligatoria es lo que
garantiza que ninguna plantilla se lo deje.

Las otras dos son `optOut.reasonLabel` y `optOut.unsubscribeLabel` del **pie de correo sin
cuenta** (`EmailOptOutGuest`), por la misma lógica: el motivo por el que a alguien sin cuenta le
llega un correo lo sabe el producto que lo manda —no el DS—, y siendo el enlace una frase entera
que cierra ese motivo, ningún default podría encajar. Obligatorias, para que ninguna plantilla
mande una baja sin decir de qué.
