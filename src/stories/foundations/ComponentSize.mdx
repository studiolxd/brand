import { Meta } from "@storybook/addon-docs/blocks";
import { TokenTable } from "./TokenTable";
import { componentSizeTokens, contentSizeTokens } from "../../tokens/size";

<Meta title="Foundations/Tallas de componente" />

# Tallas de componente

Escala de alturas para elementos de interfaz interactivos: botones de formulario, inputs, selects y otros controles. Garantiza consistencia vertical entre elementos que conviven en el mismo contexto.

## Tokens CSS

<TokenTable tokens={componentSizeTokens} />

Los tokens están definidos en `tokens/size/component.json`.

---

## Medida de contenido

Ancho máximo para bloques de texto y secciones de contenido. Garantiza legibilidad óptima y consistencia entre páginas.

<TokenTable tokens={contentSizeTokens} />

Los tokens están definidos en `tokens/size/content.json`.
