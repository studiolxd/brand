import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as LinkStories from "./Link.stories";
import { TokenTable } from "../../foundations/TokenTable";
import { linkBaseTokens, linkDarkTokens } from "../../../tokens/components/link";

<Meta of={LinkStories} />

# Link

Enlace de texto. Hereda el color del texto circundante por defecto. En reposo muestra subrayado; en hover lo oculta. La variante `external` añade `target="_blank"` con `rel="noopener noreferrer"` de forma automática.

## Por defecto

<Canvas of={LinkStories.Default} />

## External

<Canvas of={LinkStories.External} />

## Dark

<Canvas of={LinkStories.Dark} className="bg-dark" />

---

## Tokens

### Base

<TokenTable tokens={linkBaseTokens} />

### Oscuro

<TokenTable tokens={linkDarkTokens} />

## Props

<ArgTypes of={LinkStories} />
