import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as CheckboxStories from "./Checkbox.stories";
import { TokenTable } from "../../foundations/TokenTable";
import { checkboxBaseTokens, checkboxFocusTokens, checkboxDisabledTokens, checkboxDarkTokens } from "../../../tokens/components/checkbox";

<Meta of={CheckboxStories} />

# Checkbox

Control de selección binaria para formularios. Su apariencia está completamente personalizada mediante CSS, eliminando el estilo nativo del navegador.

## Sin marcar

<Canvas of={CheckboxStories.Default} />

## Marcado

<Canvas of={CheckboxStories.Checked} />

## Deshabilitado

<Canvas of={CheckboxStories.Disabled} />

## Deshabilitado y marcado

<Canvas of={CheckboxStories.DisabledChecked} />

---

## Objetivo de puntero

El dibujo mide 20px (16px en `sm`), pero el **área que responde al puntero** llega siempre
a 24×24, el mínimo de WCAG 2.5.8: la extiende una capa invisible centrada
(`checkbox.target-size`, que apunta a `--size-target-min`) que no ocupa sitio en la maqueta
ni engorda la marca.

## Tokens

### Base

<TokenTable tokens={checkboxBaseTokens} />

### Focus

<TokenTable tokens={checkboxFocusTokens} />

### Deshabilitado

<TokenTable tokens={checkboxDisabledTokens} />

### Oscuro

<TokenTable tokens={checkboxDarkTokens} />

## Props

<ArgTypes of={CheckboxStories} />
