import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as DatePickerFieldStories from "./DatePickerField.stories";
import { TokenTable } from "../../foundations/TokenTable";
import { datePickerFieldBaseTokens, datePickerFieldErrorTokens, datePickerFieldHelperTokens } from "../../../tokens/molecules/date-picker-field";

<Meta of={DatePickerFieldStories} />

# DatePickerField

El `DatePicker` como campo de formulario: etiqueta, campo con la fecha escrita, ayuda y error, con el mismo ritmo que el resto de campos. Es el que se usa en formularios; el átomo suelto, solo fuera de ellos.

Para fecha **y** hora, el campo es `DateTimeField`; para solo hora, `TimeField`.

<Canvas of={DatePickerFieldStories.PorDefecto} />

## Etiqueta

Siempre hay etiqueta (`label`), enlazada por `id` al campo, y **por defecto se ve**. Sin `id`, el campo genera uno con `useId`.

<Canvas of={DatePickerFieldStories.EtiquetaOculta} />

## Ayuda y error

`helperText` describe; `errorMessage` avisa (`role="alert"`) y pone el control en error por sí solo —`error` sirve para marcarlo sin mensaje—. Los dos quedan enlazados por `aria-describedby`, y el error también por `aria-invalid`.

<Canvas of={DatePickerFieldStories.ConAyuda} />
<Canvas of={DatePickerFieldStories.ConError} />

## Valor y rango

El campo es **controlado**: `value` + `onChange`. El formato de la fecha mostrada sale de `Intl` con el `locale` (`es-ES` por defecto), no de una plantilla propia. `minDate`, `maxDate` y `disabledDates` acotan lo elegible.

<Canvas of={DatePickerFieldStories.ConValor} />
<Canvas of={DatePickerFieldStories.ConRango} />
<Canvas of={DatePickerFieldStories.ElegirFecha} />

## Tallas

`size` viaja al control: el campo mide 32, 40 o 48, las tallas del sistema.

<Canvas of={DatePickerFieldStories.Tallas} />

## Estados

<Canvas of={DatePickerFieldStories.Deshabilitado} />
<Canvas of={DatePickerFieldStories.SoloLectura} />

## Con react-hook-form

El control es de Base UI, así que el contrato **no** es el spread del `field`: es `value` / `onChange` + `name` + `ref` al campo de texto. `onChange` entrega `Date | null` —vaciar el campo borra la fecha—, que es lo que guarda react-hook-form; el `name` monta además un input oculto con la fecha en `yyyy-mm-dd` **en el huso local** (no `toISOString()`, que es UTC y desplazaría un día) para el envío nativo.

```tsx
<FormField
  control={form.control}
  name="inicio"
  render={({ field, fieldState }) => (
    <DatePickerField
      ref={field.ref}
      name={field.name}
      value={field.value}
      onChange={field.onChange}
      onBlur={field.onBlur}
      disabled={field.disabled}
      label="Fecha de inicio"
      errorMessage={fieldState.error?.message}
    />
  )}
/>
```

<Canvas of={DatePickerFieldStories.ConReactHookForm} />

## Accesibilidad

- La etiqueta va enlazada por `htmlFor`/`id` al campo de texto; quien abre el calendario es el botón del final, con nombre accesible propio.
- Los `id` de ayuda y error se derivan del del campo (`<id>-helper`, `<id>-error`) y llegan al control por `aria-describedby`, en ese orden.
- El mensaje de error se anuncia (`role="alert"`) y marca el control con `aria-invalid`.
- Meses y días del calendario salen de `Intl` con el `locale`: no son props de texto.

## Internacionalización

El campo pone el **contenido** de la pantalla —`label`, `helperText`, `errorMessage`— y el
`BrandMessagesProvider` pone el **cromo** del control: el botón del calendario, el aviso de
fecha incompleta, las letras de la máscara y las flechas del calendario. Ninguno de esos tiene
valor por defecto, y ninguno hay que enhebrarlo desde aquí: el proveedor llega por contexto al
`DatePicker` y al `Calendar` de dentro.

<Canvas of={DatePickerFieldStories.TextosDelProveedor} />

| Qué | De dónde sale |
| --- | --- |
| `label`, `helperText`, `errorMessage` | prop, siempre: son de esta pantalla |
| botón, aviso, letras de la máscara | `datePicker.*` del proveedor |
| flechas del mes y de los años | `calendar.*` del proveedor |
| nombre del panel | la etiqueta del campo; sin ella, `datePicker.calendar` |
| meses, días y orden de la fecha | `locale`, con `Intl` — no son texto |

Las props del `DatePicker` se siguen reexponiendo tal cual (`openCalendarLabel`,
`invalidMessage`, `maskLetters`, `previousMonthLabel`, `nextMonthLabel`,
`previousYearsLabel`, `nextYearsLabel`, `yearGridLabel`, `gridLabel`) como anulación puntual
de un uso concreto. El reparto completo, con el caso de la máscara, en la doc de `DatePicker`
§ «Internacionalización»; la convención, en **Foundations › Internacionalización**.

## Tokens

El campo solo aporta el ritmo (`gap`) y los textos de ayuda y error, que referencian los de `form.*`; el control es el `DatePicker`.

### Base

<TokenTable tokens={datePickerFieldBaseTokens} />

### Error

<TokenTable tokens={datePickerFieldErrorTokens} />

### Texto de ayuda

<TokenTable tokens={datePickerFieldHelperTokens} />

## Props

<ArgTypes of={DatePickerFieldStories} />
