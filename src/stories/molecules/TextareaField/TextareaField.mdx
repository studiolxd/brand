import { Meta, Canvas, ArgTypes } from "@storybook/addon-docs/blocks";
import * as TextareaFieldStories from "./TextareaField.stories";
import { TokenTable } from "../../foundations/TokenTable";
import { textareaFieldBaseTokens, textareaFieldErrorTokens, textareaFieldHelperTokens } from "../../../tokens/molecules/textarea-field";

<Meta of={TextareaFieldStories} />

# TextareaField

El `Textarea` como campo de formulario: etiqueta, control, texto de ayuda y mensaje de error, apilados con el mismo ritmo que el resto de campos (`InputField`, `SelectField`…). Es el que se usa en formularios; el átomo suelto, solo fuera de ellos.

<Canvas of={TextareaFieldStories.PorDefecto} />

## Etiqueta

Siempre hay etiqueta (`label`), enlazada por `id`, y **por defecto se ve** —igual que en `SelectField`—. `labelHidden` la deja solo para los lectores de pantalla cuando el contexto ya dice qué es el campo (un buscador con lupa, la fila de una tabla, el correo del pie de página).

Con la etiqueta oculta y sin `placeholder` propio, el campo usa el texto de la etiqueta como placeholder: sin eso el control se quedaría sin ninguna pista visible. El nombre accesible lo sigue poniendo la etiqueta, no el placeholder.

<Canvas of={TextareaFieldStories.EtiquetaOculta} />

## Ayuda y error

`helperText` describe; `errorMessage` avisa (`role="alert"`) y pone el control en error por sí solo —`error` sirve para marcarlo sin mensaje—. Los dos quedan enlazados al control por `aria-describedby`, y el error también por `aria-invalid`. El borde en error usa el color de texto de error (`error-text-on-light`), nunca un fondo.

<Canvas of={TextareaFieldStories.ConAyuda} />
<Canvas of={TextareaFieldStories.ConError} />

## Tallas

`size` viaja al `Textarea`, que es la excepción de la regla de tallas: no fija su altura a 32/40/48 porque es multilínea. La talla cambia el cuerpo de letra y el aire; la altura la manda `rows` sobre una altura mínima.

<Canvas of={TextareaFieldStories.Tallas} />

## Estados

<Canvas of={TextareaFieldStories.ConValor} />
<Canvas of={TextareaFieldStories.Deshabilitado} />
<Canvas of={TextareaFieldStories.SuperficieOscura} />

## Accesibilidad

- La etiqueta va enlazada por `htmlFor`/`id`: `id` es obligatorio.
- Los `id` de ayuda y error se derivan del del campo (`<id>-helper`, `<id>-error`) y llegan al control por `aria-describedby`, en ese orden.
- El mensaje de error se anuncia (`role="alert"`) y marca el control con `aria-invalid`.

## Tokens

El campo solo aporta el ritmo (`gap`) y los textos de ayuda y error, que referencian los de `form.*`; el control es el `Textarea`.

### Base

<TokenTable tokens={textareaFieldBaseTokens} />

### Error

<TokenTable tokens={textareaFieldErrorTokens} />

### Texto de ayuda

<TokenTable tokens={textareaFieldHelperTokens} />

## Props

<ArgTypes of={TextareaFieldStories} />
