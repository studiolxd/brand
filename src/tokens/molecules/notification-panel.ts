import notificationPanel from '../../../tokens/molecule/notification-panel.json';
import { flattenTokens } from '../utils';

export const notificationPanelTokens = flattenTokens(notificationPanel as never);
