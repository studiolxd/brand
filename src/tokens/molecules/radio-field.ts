import radioField from '../../../tokens/molecule/radio-field.json';
import { flattenTokens } from '../utils';

const all = flattenTokens(radioField as never);

export const radioFieldBaseTokens = all;
