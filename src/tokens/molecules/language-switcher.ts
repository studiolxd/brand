import languageSwitcher from '../../../tokens/molecule/language-switcher.json';
import { flattenTokens } from '../utils';

export const languageSwitcherTokens = flattenTokens(languageSwitcher as never);
