import notificationList from '../../../tokens/molecule/notification-list.json';
import { flattenTokens } from '../utils';

export const notificationListTokens = flattenTokens(notificationList as never);
