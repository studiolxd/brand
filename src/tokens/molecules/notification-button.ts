import notificationButton from '../../../tokens/molecule/notification-button.json';
import { flattenTokens } from '../utils';

export const notificationButtonTokens = flattenTokens(notificationButton as never);
