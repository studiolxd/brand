import typingIndicator from '../../../tokens/component/typing-indicator.json';
import { flattenTokens } from '../utils';

export const typingIndicatorTokens = flattenTokens(typingIndicator as never);
