import conversationThread from '../../../tokens/molecule/conversation-thread.json';
import { flattenTokens } from '../utils';

export const conversationThreadTokens = flattenTokens(conversationThread as never);
