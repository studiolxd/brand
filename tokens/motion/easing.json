{
  "motion": {
    "easing": {
      "default": { "$value": "ease",        "$type": "string", "$description": "Transiciones de propiedad: color, fondo, opacidad" },
      "in":      { "$value": "ease-in",     "$type": "string", "$description": "Escala. El sistema no lo usa" },
      "out":     { "$value": "ease-out",    "$type": "string", "$description": "Salidas: lo que desaparece frena al final" },
      "in-out":  { "$value": "ease-in-out", "$type": "string", "$description": "Movimiento: paneles y hojas que se desplazan" },
      "linear":  { "$value": "linear",      "$type": "string", "$description": "Escala. El sistema no lo usa" }
    }
  }
}
