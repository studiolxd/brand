{
  "size-component": {
    "sm": { "$value": "2rem",   "$type": "dimension", "$description": "2rem (32px) — Small component height — compact elements" },
    "md": { "$value": "2.5rem", "$type": "dimension", "$description": "2.5rem (40px) — Medium component height — form buttons, inputs, selects" },
    "lg": { "$value": "3rem",   "$type": "dimension", "$description": "3rem (48px) — Large component height — prominent form elements" }
  }
}
