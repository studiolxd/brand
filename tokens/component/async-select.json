{
  "async-select": {
    "bg":                          { "$value": "{select.bg}", "$type": "color", "$description": "Hereda de `select.bg`: el buscador tiene la misma cara que el control síncrono" },
    "border-color":                { "$value": "{select.border-color}", "$type": "color", "$description": "Hereda de `select.border-color`: el buscador tiene la misma cara que el control síncrono" },
    "border-radius":               { "$value": "{select.border-radius}", "$type": "dimension", "$description": "Hereda de `select.border-radius`: el buscador tiene la misma cara que el control síncrono" },
    "border-style":                { "$value": "{select.border-style}", "$type": "string", "$description": "Hereda de `select.border-style`: el buscador tiene la misma cara que el control síncrono" },
    "border-width":                { "$value": "{select.border-width}", "$type": "dimension", "$description": "Hereda de `select.border-width`: el buscador tiene la misma cara que el control síncrono" },
    "color":                       { "$value": "{select.color}", "$type": "color", "$description": "Hereda de `select.color`: el buscador tiene la misma cara que el control síncrono" },
    "content-bg":                  { "$value": "{select.content-bg}", "$type": "color", "$description": "Hereda de `select.content-bg`: el buscador tiene la misma cara que el control síncrono" },
    "content-border-color":        { "$value": "{select.content-border-color}", "$type": "color", "$description": "Hereda de `select.content-border-color`: el buscador tiene la misma cara que el control síncrono" },
    "content-gap":                 { "$value": "{multi-select.content-gap}", "$type": "dimension", "$description": "Hereda de `multi-select.content-gap`: el buscador tiene la misma cara que el control síncrono" },
    "content-z-index":             { "$value": "{select.content-z-index}", "$type": "number", "$description": "Hereda de `select.content-z-index`: el buscador tiene la misma cara que el control síncrono" },
    "error-border-color":          { "$value": "{select.error-border-color}", "$type": "color", "$description": "Hereda de `select.error-border-color`: el buscador tiene la misma cara que el control síncrono" },
    "focus-ring-color":            { "$value": "{select.focus-ring-color}", "$type": "color", "$description": "Hereda de `select.focus-ring-color`: el buscador tiene la misma cara que el control síncrono" },
    "focus-ring-width":            { "$value": "{select.focus-ring-width}", "$type": "dimension", "$description": "Hereda de `select.focus-ring-width`: el buscador tiene la misma cara que el control síncrono" },
    "font-family":                 { "$value": "{select.font-family}", "$type": "string", "$description": "Hereda de `select.font-family`: el buscador tiene la misma cara que el control síncrono" },
    "font-size":                   { "$value": "{select.font-size}", "$type": "dimension", "$description": "Hereda de `select.font-size`: el buscador tiene la misma cara que el control síncrono" },
    "font-weight":                 { "$value": "{select.font-weight}", "$type": "number", "$description": "Hereda de `select.font-weight`: el buscador tiene la misma cara que el control síncrono" },
    "height":                      { "$value": "{select.height}", "$type": "dimension", "$description": "Hereda de `select.height`: el buscador tiene la misma cara que el control síncrono" },
    "item-highlighted-bg":         { "$value": "{select.item-highlighted-bg}", "$type": "color", "$description": "Hereda de `select.item-highlighted-bg`: el buscador tiene la misma cara que el control síncrono" },
    "item-padding-block":          { "$value": "{select.item-padding-block}", "$type": "dimension", "$description": "Hereda de `select.item-padding-block`: el buscador tiene la misma cara que el control síncrono" },
    "lg-font-size":                { "$value": "{select.lg-font-size}", "$type": "dimension", "$description": "Hereda de `select.lg-font-size`: el buscador tiene la misma cara que el control síncrono" },
    "lg-height":                   { "$value": "{select.lg-height}", "$type": "dimension", "$description": "Hereda de `select.lg-height`: el buscador tiene la misma cara que el control síncrono" },
    "lg-item-padding-block":       { "$value": "{select.lg-item-padding-block}", "$type": "dimension", "$description": "Hereda de `select.lg-item-padding-block`: el buscador tiene la misma cara que el control síncrono" },
    "lg-padding-inline":           { "$value": "{select.lg-padding-inline}", "$type": "dimension", "$description": "Hereda de `select.lg-padding-inline`: el buscador tiene la misma cara que el control síncrono" },
    "line-height":                 { "$value": "{select.line-height}", "$type": "number", "$description": "Hereda de `select.line-height`: el buscador tiene la misma cara que el control síncrono" },
    "padding-inline":              { "$value": "{select.padding-inline}", "$type": "dimension", "$description": "Hereda de `select.padding-inline`: el buscador tiene la misma cara que el control síncrono" },
    "placeholder-color":           { "$value": "{multi-select.placeholder-color}", "$type": "color", "$description": "Hereda de `multi-select.placeholder-color`: el buscador tiene la misma cara que el control síncrono" },
    "sm-font-size":                { "$value": "{select.sm-font-size}", "$type": "dimension", "$description": "Hereda de `select.sm-font-size`: el buscador tiene la misma cara que el control síncrono" },
    "sm-height":                   { "$value": "{select.sm-height}", "$type": "dimension", "$description": "Hereda de `select.sm-height`: el buscador tiene la misma cara que el control síncrono" },
    "sm-item-padding-block":       { "$value": "{select.sm-item-padding-block}", "$type": "dimension", "$description": "Hereda de `select.sm-item-padding-block`: el buscador tiene la misma cara que el control síncrono" },
    "sm-padding-inline":           { "$value": "{select.sm-padding-inline}", "$type": "dimension", "$description": "Hereda de `select.sm-padding-inline`: el buscador tiene la misma cara que el control síncrono" },
    "item-selected-font-weight":   { "$value": "{font-weight.emphasis}", "$type": "number", "$description": "Peso de la opción ya elegida dentro de la lista de resultados" },
    "loading-padding-block":       { "$value": "{select.item-padding-block}", "$type": "dimension", "$description": "Aire vertical de la fila de carga: la misma caja que una opción, para que la lista no salte al llegar los resultados" },
    "loading-padding-inline":      { "$value": "{select.padding-inline}", "$type": "dimension", "$description": "Aire horizontal de la fila de carga" },
    "empty-padding-block":         { "$value": "{select.item-padding-block}", "$type": "dimension", "$description": "Aire vertical del mensaje de lista vacía" },
    "empty-padding-inline":        { "$value": "{select.padding-inline}", "$type": "dimension", "$description": "Aire horizontal del mensaje de lista vacía" },
    "empty-font-family":           { "$value": "{select.font-family}", "$type": "string", "$description": "Tipografía del mensaje de lista vacía" },
    "empty-font-size":             { "$value": "{select.font-size}", "$type": "dimension", "$description": "Tamaño del mensaje de lista vacía" },
    "empty-color":                 { "$value": "{color.text.muted-on-light}", "$type": "color", "$description": "Tinta del mensaje de lista vacía: es texto secundario, no un valor" },
    "surface-dark-empty-color":    { "$value": "{color.text.muted-on-dark}", "$type": "color", "$description": "Tinta del mensaje de lista vacía — surface-dark" },
    "surface-dark-item-highlighted-bg": { "$value": "{select.surface-dark-item-highlighted-bg}", "$type": "color", "$description": "Relleno de la opción resaltada — surface-dark" }
  }
}
